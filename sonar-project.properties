# must be unique in a given SonarQube instance
# <Org Name>:<Repo Name> convention
sonar.projectKey=velox:trawler

# this is the name displayed in the SonarQube UI
# use the same value from sonar.projectKey
sonar.projectName=velox:trawler

# Path is relative to the sonar-project.properties file
sonar.sources=.
sonar.exclusions=testdata/**,node_modules/**,htmlcov/**,__pycache__/**,coverage.xml,**/test_*.py
sonar.python.coverage.reportPath=coverage.xml
